describe("UNIT: AOT Compiler: ", () => {

    it("should produce a bundle of user-defined code, converted to the target language", (): void => {
        expect(true).toBe(false);
    });

    it("should compile built-in objects and functions and include them in the bundle", (): void => {
        expect(true).toBe(false);
    });



});
